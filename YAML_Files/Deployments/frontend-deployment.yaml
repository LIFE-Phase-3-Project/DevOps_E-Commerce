apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: e-commerce
  labels:
    app: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: europe-west3-docker.pkg.dev/e-commerce-life-2/frontend-repo/frontend:2b39acc
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: REACT_APP_FIREBASE_API_KEY
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_FIREBASE_API_KEY
        - name: REACT_APP_AUTHDOMAIN
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_AUTHDOMAIN
        - name: REACT_APP_PROJECT_ID
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_PROJECT_ID
        - name: REACT_APP_STORAGEBUCKET
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_STORAGEBUCKET
        - name: REACT_APP_MESSAGING_SENDER_ID
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_MESSAGING_SENDER_ID
        - name: REACT_APP_APP_ID
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_APP_ID
        - name: REACT_APP_MEASURMENT_ID
          valueFrom:
            secretKeyRef:
              name: firebase-secrets
              key: REACT_APP_MEASURMENT_ID
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: e-commerce
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  namespace: e-commerce
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80